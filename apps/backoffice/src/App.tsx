/**
 * Back-office shell — currently hosts the design-system Showcase (T0.6).
 * Dashboards replace this as the home screen in M6.
 */
import { Showcase } from './Showcase';

export function App(): JSX.Element {
  return <Showcase />;
}
