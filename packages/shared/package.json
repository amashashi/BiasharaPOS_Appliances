{
  "name": "@biashara/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types and platform-adapter contracts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
