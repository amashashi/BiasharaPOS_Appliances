# Context — BiasharaPOS Appliances & Electronics Platform

> One-page cold-start snapshot. Read this first; a minute here replaces the whole conversation history.

**Last refreshed:** 2026-07-16 (building on the user's Windows machine, Claude Code)

## What this is
Independent SaaS platform for **appliance & electronics retailers in Tanzania** — a new vertical product in the BiasharaPOS family, built as its own codebase that borrows the platform services (TRA VFD fiscalization, mobile money, identity, SMS) via API adapters. V1 = the three things generic POS can't do: **serialized inventory, the scheduled sale (deposit → delivery → installation), and the installment/layaway credit ledger** (digitized "mali kauli"). Docs: `PROJECT_BRIEF.md` → `ARCHITECTURE.md` → `PLAN.md` (approved 2026-07-15) → `DECISIONS.md` → `DESIGN_SYSTEM.md` (v2). Deeper history (market research, brainstorm) lives in the claude.ai project "Home Appliance and Electroni POS".

## Where we are
- **M0 COMPLETE** (T0.1–T0.7: scaffold+CI, data layer, adapter contracts+stubs, auth guard, audit log, design tokens+ui, brand assets).
- **T1.1 DONE** — Catalog module: products table + CRUD (OWNER writes, any-auth reads, soft archive) + CSV import with per-row `{line, errors[]}` reporting (D-018); 50-row fixture at `apps/api/test/fixtures/products-sample-50.csv`.
- **T1.2 DONE** — InventoryModule: `POST /grns` receives stock atomically (all-or-nothing, D-019), one `SerializedUnit(IN_STOCK)` per serial, `UNIQUE(merchantId, serial)`, duplicate serials rejected naming each offender + field path; GRN provenance readable via `GET /grns/:id`.
- **T1.3 DONE** — `UnitStateService.transition()` is THE only way to change `serialized_units.status` (FOR UPDATE lock, legal-graph check, status+audit in one transaction; illegal → 409, logs nothing; no shortcut edges — direct sale = RESERVE then SELL, D-021).
- **T1.4 DONE** — `GET /stock` (product × location status counts, filters) + `GET /units/lookup?serial=` (unit + GRN provenance + full actor-attributed history); any-authenticated reads. UI deferred to T6.1 (D-022).
- **T1.5 DONE — M1 COMPLETE** — `products.isSerialized` (immutable once stock exists) + `stock_levels` qty ledger; mixed GRNs (serials[] vs qty per line, type-shaped validation); stock view shows both models. 84 tests green. Neon production + development branches both migrated through 1784160000000 (prod applied 2026-07-16 with explicit owner go).
- Conventions (from vitest/esbuild having no decorator metadata): explicit types on every `@Column`; **explicit `@Inject(token)` on every constructor param**; DI tokens live in `tokens.ts` files, never in module files (CJS circular-import trap, see T1.1 surprises).
- **T2.1 DONE (M2 begins)** — OrdersModule: customers + sales orders (per-merchant sequential numbers), QUOTE/ORDER creation with inline customer, service lines, single-door lifecycle (`order-state.ts`, full ARCHITECTURE graph), computed totals, quote PDF via pdfkit + `@biashara/ui/tokens` (D-023). 91 tests green. Neon dev+prod schema-current (6 migrations); **post-merge migrations to both branches are standing policy (D-024): `npm run migrate -w apps/api` then `npm run migrate:prod -w apps/api`**.
- **T2.2 DONE** — FulfillmentService: reserve serials on CONFIRMED orders (`orderLineId` link), fulfill with deferred picks (RESERVE→SELL composition) + qty-stock decrement, cancel releases reservations — all single-transaction. `audit_events.seq` (bigserial) added: **history queries order by seq, never `at`** (transaction-stable timestamps tie). 101 tests green.
- **T2.3 DONE** — Payments ledger: append-only `payments` table (DB trigger, like audit), CASH recording with computed balance + overpayment rejection, DEPOSIT/FULL_SETTLEMENT audit tagging, reversing-entry corrections (`reversesPaymentId` UNIQUE), auto-close when fulfilled+paid; order totals now include `paidTzs`/`balanceTzs`. Fiscal receipts get their own table in T2.4 (D-025). 107 tests green.
- **T2.4 DONE** — FiscalModule: payments enqueue BullMQ jobs (post-commit, idempotent by paymentId), worker issues stub VFD receipts with exponential-backoff retries, `fiscal_receipts` table, 80mm HTML receipt endpoint (brand header + QR, reprint = re-read). Deposits fiscalize as summary line (D-026, provisional). **Local Redis = Memurai service on :6379** (already installed for core biashara-pos); CI has a redis:7 service. 111 tests green.
- **T2.5 DONE** — Mobile money at POS: `payment_intents` (PENDING→CONFIRMED/FAILED, idempotent webhook), STK push via the PaymentsService port with pre-push balance validation, webhook applies money through the shared `applyPayment` door + fiscalizes via the T2.4 queue; settled-while-pending money held CONFIRMED-unapplied for reconciliation (D-027). `/webhooks/payments` is @Public until T5.3 signature auth. 117 tests green.
- **T2.6 DONE — M2 COMPLETE** — POS checkout is a working UI: dev sign-in (D-028 scaffolding, `DEV_AUTH=off` disables, dies at T5.1), search/cart/customer, cash full+deposit and mobile-money (with pending-state polling + dev approve buttons), fiscal receipt iframe with print. Verified live on tablet viewport against Neon dev + Memurai: cash sale and deposit sale each well under 90s. API has permissive CORS (stub era). 122 tests green.
- **T3.1 DONE (M3 begins)** — CreditModule: agreements snapshot the order (principal = total, deposit = paid-so-far, schedule finances the rest — enforced identity, D-029); equal-monthly (month-end-clamped, remainder-exact) or custom schedules; INSTALLMENT releases goods, LAYAWAY 409s fulfillment until SETTLED; ACTIVE agreements block order cancel; `credit_schedule_rows.paidTzs` ready for T3.2. 131 tests green.
- **T3.2 DONE** — Schedule payment application: a payment-applied hook on PaymentsService (D-030) fires inside the payment transaction; ScheduleApplicationService distributes oldest-due-first (reversals newest-first) via pure `applyDelta`, sets row PAID/PARTIAL/PENDING, settles the agreement at full coverage (auto-unlocks layaway via T3.1's gate) and reopens on reversal. Cash + mobile-money + reversals all flow through it. 142 tests green. No schema change.
- **T3.3 DONE** — Arrears engine: nightly BullMQ job (`ArrearsService.recomputeOverdue`, Memurai; `ARREARS_CRON=off` disables) flips past-due unpaid rows → OVERDUE; `GET /credit/arrears?asOf=&sort=` computes arrears live from dates (D-031), OWNER-only; **back-office screen is now live** (`apps/backoffice`: dev sign-in → sortable arrears table with severity-ramp day badges, replaced the Showcase as home). Every method takes explicit `asOf` for fake-clock tests. 151 tests green. No migration.
- **Back-office app shell live (owner-requested, 2026-07-17/18)** — `SideNav` in packages/ui + hash-routed `Shell.tsx`: modules register once and fill in one by one. Live: Mikopo/Credit (arrears), Muundo/Design (Showcase), **Fanya Mauzo/Make a Sale** (link-out to the POS, `VITE_POS_URL`); coming-soon: Dashboard/Catalog/Stock/Orders (M6), Deliveries (M4). Design pass: dd/MM/yyyy everywhere user-facing (`formatDate` in `@biashara/ui/i18n`), bilingual PDF headers. **Locale: default ENGLISH, persists per device via `usePersistedLocale` (D-032).** 153 tests green.
- **T3.4 DONE** — Reminders: per-merchant offsets (`merchants.reminderOffsetsDays`, default [-2,0,3]) + `reminder_logs` with UNIQUE(row, offset) at-most-once claims (D-033: strict-day match, 07:00 job `REMINDERS_CRON`, FAILED never auto-retried); `GET/PUT /credit/reminder-policy`; agreement GET carries the log; back-office arrears rows drill into an agreement screen (schedule + reminder log). 159 tests green.
- **T3.5 DONE — M3 COMPLETE** — Customer statement PDF: `GET /orders/:id/statement.pdf` (A4, bilingual, schedule + payment history incl. reversals + balance); paid/balance computed from the ledger (never stored) so the statement can't disagree. Testkit `src/testkit/pdf.ts` (shared PDF-text decoder). 162 tests green.
- **T4.1 DONE (M4 begins)** — DeliveryModule: `POST/GET /orders/:id/delivery`; schedule from a schedulable order (date/window/address/assignee), PLANNED status, double-booking blocked by app-guard + partial unique index `WHERE status<>'FAILED'`; order read carries a `delivery` field (`SalesOrder.deliveries` relation). 166 tests green.
- **T4.2 DONE** — Dispatch list: `GET /deliveries/dispatch?date=` + `POST /deliveries/:id/dispatch` (`@Roles('OWNER','DELIVERY')`); DELIVERY staff see only their assigned jobs, OWNER sees all; mark PLANNED→DISPATCHED (audited). Back-office `Dispatch.tsx` mobile screen (tel: Call, job cards); dev login role picker; single-destination users get a slim top bar (no sidebar). `RemindersService.dispatchDue` gained optional merchantId scope; vitest testTimeout 20s. 171 tests green.
- **T4.3 DONE — M4 COMPLETE** — Proof of delivery: `POST /deliveries/:id/confirm` (proof required, serials must match SOLD units, SOLD→DELIVERED via UnitStateService, proof stored) + `/fail` (reason, frees reschedule). Dispatch job carries SOLD serials; UI dispatch card has ✓ Delivered / ✗ Failed inline forms. 174 tests green.
- **T5.1 DONE (D-034)** — Identity is real: `PlatformIdentityService` verifies bearer tokens via the platform's `GET /auth/me` (no shared secret; 60s cache); `businessId`→merchant via `merchants.platformBusinessId`; `/auth/login`+`/auth/refresh` proxies (rotating refresh, single-flight retry in both frontends); role map admin/manager→OWNER, cashier→CASHIER, delivery→DELIVERY, unmapped rejected. D-028 dev-identity scaffolding DELETED; new phone+PIN `Login.tsx` in both apps. `mm-resolve` stub-payments simulator kept but secure-by-default (off under `IDENTITY_MODE=platform`), dies at T5.3. 187 tests; live-verified against prod (GWA Electronics test business → OWNER / Demo Electronics Ltd).
- **T5.3a DONE (D-035)** — Reconciliation view: `payment_webhook_events` queue; unknown-intent webhooks recorded + 200'd (not 404-looped); confirmed-but-unapplied money (D-027) lands in `GET /reconciliation` (OWNER); resolve-with-note clears + audits. Back-office Reconciliation module. Platform has **no** mobile-money push API → rail is the **ClickPesa aggregator** (owner-approved). 189 tests; browser-verified.
- **➡ Next action: T5.3b** — ClickPesa adapter: token (`/generate-token`, client-id+api-key) + checksum (HMAC-SHA256 canonical) + initiate-USSD-push + signed webhook parse; env-branched binding (`PAYMENTS_MODE=clickpesa`); fake-ClickPesa-server test. **Live per-provider sandbox e2e BLOCKED on owner-provisioned ClickPesa sandbox credentials** (client-id, api-key, checksum key). **T5.2 (real fiscal) stays BLOCKED on the TRA compliance answer.** (Backlog: design re-audit that hit the session cap; deferred stock/serial-lookup UI D-022; deferred auth hardening — refresh token in localStorage, matches the platform's own web app.)

## The plan in one breath
M0 foundations & contracts → M1 serialized inventory → M2 scheduled sales + POS checkout (stub fiscal) → M3 installment/layaway ledger → M4 delivery → M5 swap stubs for real platform APIs + offline outbox → M6 back office, i18n, design-partner UAT (30-day pilot gate).

## Repo & environment
- **GitHub: `amashashi/BiasharaPOS_Appliances`** (user's working copy: `C:\BiasharaPOS_Appliances`). GitHub live since e613305 (user pushed). Update flow: bundle → user `git pull claude-history.bundle main` → `git push`.
- npm workspaces: `apps/api` (NestJS 11, plain tsc, prefix `/api`), `apps/pos` (:5173 PWA shell), `apps/backoffice` (:5174), `packages/shared`, `packages/ui`.
- Commands: `npm ci` → `npm run build --workspace packages/shared` → `npm run lint` / `npm run build` / `npm test`; `npm run migrate | migrate:down | seed -w apps/api`.
- **Databases (D-020):** Neon project `biashara-appliances` (`green-unit-19592753`, eu-central-1, PG16) with branch-per-environment — `production` (URL only in deploy env vars; nothing deployed yet) and `development` (URL in gitignored `apps/api/.env`, read by `npm run dev/start`; both branches migrated, dev seeded 2026-07-16). **Tests default to localhost** (vitest doesn't read `.env`): embedded Postgres 16.13 under `~/.biashara-devdb` (D-017) — start from PowerShell, NOT git-bash: `& "$env:USERPROFILE\.biashara-devdb\node_modules\@embedded-postgres\windows-x64\native\bin\pg_ctl.exe" -D "$env:USERPROFILE\.biashara-devdb\data" -l "$env:USERPROFILE\.biashara-devdb\pg.log" start`. CI uses its `postgres:16` service; docker-compose for machines with a daemon.

## Watch out for
- **Prisma is unusable on restricted networks** (engine downloads blocked) — data layer is TypeORM with handwritten SQL migrations (D-014). Don't reintroduce Prisma.
- **Design source of truth** is `design-handoff/` (azure blue #0F5DA4 primary, green #239B46 CTA, gold accent, Plus Jakarta Sans) — NOT the v1 green-primary/Inter assumptions (D-015). Logo: repo `brand/*.svg` are canonical (D-016). No hex literals in feature code once T0.6 lands.
- Money = integer TZS only; serial state changes only through the state machine; every payment fiscalizes via the FiscalService port (per-payment assumption, D-008 provisional).
- Sessions now run directly on the user's machine: commits work normally (the old `.git/index.lock` cowork-bridge quirk is gone; no lock present as of 2026-07-16). Pushes still run by the user.

## Open items owned by the human
1. `git push origin main` (local commits from 2026-07-16 onward).
2. TRA compliance ruling on installment fiscalization (blocks T5.2 only).
3. Dealer validation workstream (existing-customer evidence + Kariakoo shop visits).
